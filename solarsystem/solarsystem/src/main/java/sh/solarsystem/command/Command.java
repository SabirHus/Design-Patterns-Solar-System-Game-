package sh.solarsystem.command;

/**
 * Represents a generic command that can be executed,
 * typically triggered by a button or control.
 */
public interface Command {
    void execute();
}
